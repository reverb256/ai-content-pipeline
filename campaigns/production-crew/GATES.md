G1: All 8 production bot profiles exist with role contracts  |  CHECK: cd /home/j_kro/Projects/ai-content-pipeline && for b in researcher scriptwriter voicebot videobot thumbnailbot seobot publishbot analyst; do test -f profiles/$b/role.md || exit 1; done && echo ALL_ROLES_PRESENT  |  EXPECT: ALL_ROLES_PRESENT
G2: All 8 bot profiles deployed to ~/.hermes/profiles  |  CHECK: for b in researcher scriptwriter voicebot videobot thumbnailbot seobot publishbot analyst; do test -f ~/.hermes/profiles/$b/SOUL.md || exit 1; done && echo ALL_SOULS_DEPLOYED  |  EXPECT: ALL_SOULS_DEPLOYED
G3: All 8 bots have a working free model  |  CHECK: cd /home/j_kro/Projects/ai-content-pipeline && for b in researcher scriptwriter voicebot videobot thumbnailbot seobot publishbot analyst; do hermes config get -p $b model.default 2>/dev/null | grep -qE "longcat|minimax|nemotron" || exit 1; done && echo ALL_MODELS_SET  |  EXPECT: ALL_MODELS_SET
G4: All 8 bots have SPOC skills delegation  |  CHECK: for b in researcher scriptwriter voicebot videobot thumbnailbot seobot publishbot analyst; do hermes config get -p $b skills.external_dirs 2>/dev/null | grep -q hermes/skills || exit 1; done && echo ALL_SKILLS_DELEGATED  |  EXPECT: ALL_SKILLS_DELEGATED
G5: Video production stack verified (manim + video tools)  |  CHECK: export PATH=$HOME/.local/bin:$PATH && manim --version 2>/dev/null | grep -q "Manim Community" && echo MANIM_OK  |  EXPECT: MANIM_OK
G6: Voice routing verified (pick-provider voice)  |  CHECK: cd /home/j_kro/Projects/ai-content-pipeline && scripts/api/pick-provider.sh voice 2>/dev/null | grep -qE "xai|chatterbox" && echo VOICE_ROUTED  |  EXPECT: VOICE_ROUTED
G7: Pipeline stages exist on faceless-youtube kanban board  |  CHECK: hermes kanban --board faceless-youtube list 2>/dev/null | head -1 && echo BOARD_EXISTS  |  EXPECT: BOARD_EXISTS
G8: All work committed and pushed  |  CHECK: cd /home/j_kro/Projects/ai-content-pipeline && git status --porcelain | wc -l | grep -q '^0$' && git log origin/main -1 --format=%s | grep -qiE "roles|bots|production" && echo COMMITTED_PUSHED  |  EXPECT: COMMITTED_PUSHED
